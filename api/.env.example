# =============================================================================
# PakAura Backend Configuration
# =============================================================================
# SECURITY: Never commit actual API keys or secrets to version control!
# Copy this file to .env and fill in your actual values.

# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Environment
ENVIRONMENT=development
DEBUG=true

# =============================================================================
# AI Assistant Configuration (Phase 3) - Cohere FREE Tier
# =============================================================================
# Get your FREE API key at: https://dashboard.cohere.com/api-keys
#
# SECURITY NOTES:
# - NEVER hardcode API keys in source code
# - NEVER commit .env files to version control
# - API key is loaded from this environment variable only
#
# DEMO MODE:
# - If COHERE_API_KEY is empty, chatbot runs in DEMO MODE
# - Demo mode still allows basic task operations via keyword detection
# - Full AI responses require a valid Cohere API key
#
COHERE_API_KEY=
AI_PROVIDER=cohere
AI_MODEL=command-a-03-2025
AI_TEMPERATURE=0.3
AI_MAX_TOKENS=300
AI_TIMEOUT_SECONDS=30
AI_RATE_LIMIT_PER_MINUTE=30
AI_MAX_INPUT_LENGTH=10000
