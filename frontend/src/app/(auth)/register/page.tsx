/**
 * Register page route.
 * T036: Create register page route structure
 */

import { RegisterForm } from '@/components/auth';

export default function RegisterPage() {
  return <RegisterForm />;
}
